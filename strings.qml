// This is a fake qml file to hold translation ids that can't be declared
// where they're really used (for example the one in the .desktop file).
// They're here so that lupdate will see them.

Component {
    //: Application name in desktop file
    //% "Notes"
    name: qsTrId("notes-de-name")
}
